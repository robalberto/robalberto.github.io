---
target: index.html
total_score: 36
p0_count: 0
p1_count: 0
timestamp: 2026-06-13T11-15-02Z
slug: index-html
---
#### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | No active-state indicator on nav links |
| 2 | Match System / Real World | 3 | Timelines read naturally; highlight icons still somewhat generic |
| 3 | User Control and Freedom | 4 | Standard anchor scrolling, skip-link present |
| 4 | Consistency and Standards | 4 | Cohesive typography system, consistent spacing tokens |
| 5 | Error Prevention | 4 | N/A (read-only surface) |
| 6 | Recognition Rather Than Recall | 4 | Persistent nav, clear section structure |
| 7 | Flexibility and Efficiency | 3 | Standard web conventions, no accelerators |
| 8 | Aesthetic and Minimalist Design | 3 | Distinctive voice; a few dead CSS rules remain |
| 9 | Error Recovery | 4 | N/A |
| 10 | Help and Documentation | 4 | N/A |
| **Total** | | **36/40** | **Good** |

#### Anti-Patterns Verdict

**LLM assessment**: The site maintains its committed visual identity. The Blog nav link now uses the terminal command style (JetBrains Mono with `>` prefix), adding personality to the nav. The connected highlights list, hero code snippet overlay, and Outfit + JetBrains Mono pairing all remain strong. No new AI tells introduced. The page reads as a deliberate personal brand.

**Deterministic scan**: The detector found 1 issue (unchanged from last run):
1. **"Single font for everything" (Warning)**: Still a **false positive**. The page loads both Outfit and JetBrains Mono via Google Fonts. The detector only detects JetBrains Mono in the URL. Two distinct font families are in active use.

#### Overall Impression
Stable. The live mode iteration on the Blog link was a good addition — the terminal command style fits the brand personality. The remaining issues are unchanged from the last critique: a heading that overshoots the ceiling, a generic bounce animation, missing mobile nav, and dead CSS.

#### What's Working
- **Terminal Blog link**: The `> Blog` mono link in the nav adds a distinctive touch that reinforces the technical identity without being overdone.
- **Connected highlights list**: Still a strong structural improvement — reads as a deliberate sequence.
- **Typography pairing**: Outfit + JetBrains Mono creates genuine contrast. Not a reflex pick.
- **Hero visual weight**: Code snippet overlay, masked grid lines, and accent glow give the hero a committed identity.

#### Priority Issues

**[P2] Hero h1 exceeds display ceiling**
- **Why it matters**: The hero h1 uses `clamp(4rem, 8vw, 7rem)`. The 7rem max (~112px) exceeds the recommended 6rem ceiling. On large viewports, the name will feel like it's shouting rather than designing.
- **Fix**: Cap the clamp max at `6rem`: `clamp(4rem, 8vw, 6rem)`.
- **Suggested command**: `/impeccable typeset index.html`

**[P2] Bounce animation on scroll-down chevron**
- **Why it matters**: The `@keyframes bounce` on `.scroll-down` is a generic, overused animation pattern. Also lacks a `prefers-reduced-motion` alternative.
- **Fix**: Replace with a subtle, single-direction fade-and-slide that respects `prefers-reduced-motion`, or remove the animation entirely.
- **Suggested command**: `/impeccable animate index.html`

**[P2] No mobile navigation**
- **Why it matters**: `.nav-links` is set to `display: none` on mobile with no hamburger menu or alternative. Mobile users lose all navigation context.
- **Fix**: Add a hamburger toggle that reveals nav links on mobile, or convert to a horizontal scrollable nav strip.
- **Suggested command**: `/impeccable adapt index.html`

**[P3] Dead CSS rules from old layout**
- **Why it matters**: `.highlight-card`, `.highlight-card:hover`, `.highlight-card i`, `.highlight-card p`, `.cards-grid`, `.card`, `.card:hover`, `.card-header`, `.year`, `.card ul`, `.card li` are all orphaned rules from the old card grid and projects section.
- **Fix**: Remove all dead rules.
- **Suggested command**: `/impeccable polish index.html`

**[P3] Duplicate `p` rule in CSS**
- **Why it matters**: Two separate `p` selectors exist (one with `max-width`/`text-wrap`, another with `margin-bottom`). The second overrides nothing but creates maintenance confusion.
- **Fix**: Merge into a single `p` rule.
- **Suggested command**: `/impeccable polish index.html`

**[P3] Navbar background color mismatch**
- **Why it matters**: The `.navbar` uses `rgba(15, 23, 42, 0.85)` which doesn't match the updated `--bg-color: #0b0f19`. Should be `rgba(11, 15, 25, 0.85)`.
- **Fix**: Update the navbar background to match the new palette.
- **Suggested command**: `/impeccable polish index.html`

#### Persona Red Flags

**Alex (Power User / Tech Recruiter)**:
- GitHub link is present in the hero. The commented-out "Projects" nav link still creates a gap between claims and proof.
- No keyboard shortcut or quick-scan affordance for jumping to the blog.

**Casey (Distracted Mobile User)**:
- Navigation is completely hidden on mobile (`display: none`). No hamburger, no alternative. Casey has to scroll the entire page to find anything.
- Touch targets on the contact-info links may be tight on small screens.

#### Minor Observations
- The `scroll-behavior: smooth` on `html` combined with the JS smooth-scroll handler creates double-smoothing. Pick one.
- The blog section has two `bg-alt` sections back-to-back (blog + skills) with no visual break between them.
- The `.highlight-card` CSS rules are completely dead — no elements use that class anymore.

#### Questions to Consider
- What if the mobile nav used a full-screen overlay instead of a hamburger dropdown, matching the bold hero aesthetic?
- Could the commented-out "Projects" section be revived as a showcase for the blog posts?
- What would a `prefers-reduced-motion` version of the hero look like? (Static code snippet, no bounce, instant load.)
